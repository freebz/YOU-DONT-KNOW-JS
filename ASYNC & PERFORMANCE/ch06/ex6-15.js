var x = "42"; // 숫자 '42'가 필요하다.

// 옵션 1: 자동으로 암시적 강제변환이 일어나게 한다.
var y = x / 2;

// 옵션 2: 'parseInt( )'를 쓴다.
var y = parseInt( x, 0 ) / 2;

// 옵션 3: 'Number()'를 쓴다.
var y = Number( x ) / 2;

// 옵션 4: '+' 단항 연산자를 쓴다.
var y = +x / 2;

// 옵션 5: '|' 단항 연산자를 쓴다.
var y = (x | 0) / 2;
